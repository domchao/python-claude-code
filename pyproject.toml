[project]
name = "python-claude-code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "domchao", email = "d.chaopradith@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
python-claude-code = "python_claude_code:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
